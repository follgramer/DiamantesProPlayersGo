<?xml version="1.0" encoding="utf-8"?>
<androidx.drawerlayout.widget.DrawerLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/drawer_layout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/primary_bg"
    android:fitsSystemWindows="true"
    tools:context=".MainActivity">

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:fitsSystemWindows="true">

        <androidx.appcompat.widget.Toolbar
            android:id="@+id/toolbar"
            android:layout_width="match_parent"
            android:layout_height="56dp"
            android:layout_alignParentTop="true"
            android:background="@color/secondary_bg"
            android:elevation="4dp">

            <RelativeLayout
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:layout_marginEnd="16dp">

                <FrameLayout
                    android:id="@+id/notificationButton"
                    android:layout_width="48dp"
                    android:layout_height="match_parent"
                    android:layout_alignParentEnd="true"
                    android:background="?attr/selectableItemBackgroundBorderless"
                    android:clickable="true"
                    android:focusable="true">

                    <ImageView
                        android:layout_width="24dp"
                        android:layout_height="24dp"
                        android:layout_gravity="center"
                        android:src="@drawable/ic_notification"
                        android:tint="@color/text_color" />

                    <TextView
                        android:id="@+id/notificationBadge"
                        android:layout_width="20dp"
                        android:layout_height="20dp"
                        android:layout_gravity="top|end"
                        android:layout_marginTop="8dp"
                        android:layout_marginEnd="8dp"
                        android:background="@drawable/badge_background"
                        android:gravity="center"
                        android:text="0"
                        android:textColor="@android:color/white"
                        android:textSize="11sp"
                        android:textStyle="bold"
                        android:visibility="gone" />
                </FrameLayout>
            </RelativeLayout>
        </androidx.appcompat.widget.Toolbar>

        <!-- Banner inferior - ANTES del contenido para asegurar orden correcto -->
        <FrameLayout
            android:id="@+id/bannerBottomContainer"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_alignParentBottom="true"
            android:visibility="gone" />

        <!-- Contenedor principal - ENCIMA del banner inferior -->
        <FrameLayout
            android:id="@+id/fragment_container"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_below="@id/toolbar"
            android:layout_above="@id/bannerBottomContainer">

            <include
                android:id="@+id/section_home"
                layout="@layout/section_home" />

            <include
                android:id="@+id/section_tasks"
                layout="@layout/section_tasks"
                android:visibility="gone" />

            <include
                android:id="@+id/section_leaderboard"
                layout="@layout/section_leaderboard"
                android:visibility="gone" />

            <include
                android:id="@+id/section_winners"
                layout="@layout/section_winners"
                android:visibility="gone" />

            <include
                android:id="@+id/section_legal"
                layout="@layout/section_legal"
                android:visibility="gone" />
        </FrameLayout>

    </RelativeLayout>

    <!-- Navigation Drawer -->
    <LinearLayout
        android:layout_width="280dp"
        android:layout_height="match_parent"
        android:layout_gravity="start"
        android:background="@color/secondary_bg"
        android:fitsSystemWindows="true"
        android:orientation="vertical">

        <include layout="@layout/nav_header" />

        <com.google.android.material.navigation.NavigationView
            android:id="@+id/nav_view"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1"
            android:background="@color/secondary_bg"
            app:menu="@menu/navigation_menu"
            app:itemIconTint="@color/accent_color"
            app:itemTextColor="@color/text_color" />

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="@color/secondary_bg"
            android:orientation="vertical"
            android:paddingVertical="8dp">

            <View
                android:layout_width="match_parent"
                android:layout_height="1dp"
                android:layout_marginHorizontal="16dp"
                android:layout_marginBottom="8dp"
                android:alpha="0.3"
                android:background="@color/border_color" />

            <TextView
                android:id="@+id/nav_privacy"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:background="?attr/selectableItemBackground"
                android:clickable="true"
                android:focusable="true"
                android:gravity="center_vertical"
                android:minHeight="48dp"
                android:paddingHorizontal="32dp"
                android:paddingVertical="12dp"
                android:text="@string/privacy_policy"
                android:textColor="#C0C0C0"
                android:textSize="13sp" />

            <TextView
                android:id="@+id/nav_terms"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:background="?attr/selectableItemBackground"
                android:clickable="true"
                android:focusable="true"
                android:gravity="center_vertical"
                android:minHeight="48dp"
                android:paddingHorizontal="32dp"
                android:paddingVertical="12dp"
                android:text="@string/terms_conditions"
                android:textColor="#C0C0C0"
                android:textSize="13sp" />
        </LinearLayout>
    </LinearLayout>

</androidx.drawerlayout.widget.DrawerLayout>